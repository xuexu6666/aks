# RuntimeClass mapping pods to the nvidia containerd runtime handler.
# Pods that set runtimeClassName: nvidia get the prestart-hook lib/device
# injection from nvidia-container-runtime.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
